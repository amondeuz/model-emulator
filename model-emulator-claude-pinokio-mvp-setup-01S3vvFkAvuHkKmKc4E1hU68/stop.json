{
  "run": [
    {
      "method": "shell.run",
      "params": {
        "message": "{{platform === 'win32' ? 'taskkill /F /IM node.exe' : 'pkill -f \"node server/index.js\"'}}"
      }
    },
    {
      "method": "log",
      "params": {
        "raw": "Server stopped"
      }
    }
  ]
}
